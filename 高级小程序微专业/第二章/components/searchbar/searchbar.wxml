<view class="app-searchbar">
  <icon type="search"/>
  <input confirm-type="search" placeholder="{{placeholder}}" bindinput="inputChange" value="{{searchText}}"></input>
  <button size="mini" bindtap="tapSearch">搜索</button>
</view>